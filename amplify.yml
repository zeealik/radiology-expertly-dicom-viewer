version: 1
applications:
  - appRoot: platform/app
    frontend:
      buildPath: /
      phases:
        preBuild:
          commands:
            - yarn install --frozen-lockfile
        build:
          commands:
            - NODE_ENV=production APP_CONFIG=config/radiologyexpertly.js PUBLIC_URL=/ yarn workspace @ohif/app build
      artifacts:
        baseDirectory: platform/app/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
          - platform/app/node_modules/**/*
